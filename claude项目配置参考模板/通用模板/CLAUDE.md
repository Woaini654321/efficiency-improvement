# CLAUDE.md — {{项目名称}}（{{仓库名/代号}}）

最后更新：{{YYYY-MM-DD}}

> 本文件是本仓库对 Claude Code 的强制规则，优先级高于默认行为。
> 全局个人偏好仍适用，但**本文件与全局偏好冲突时，以本文件为准**。
>
> **模板使用说明（用前必读，填完后删除本段）**：
> 1. 全文搜索 `{{` 逐个替换为本项目实际值；`> 说明：` 开头的引用块是填写指引，填完删除。
> 2. 标注「可选」的章节按项目需要保留或整节删除，不要留空壳。
> 3. 每一条写进来的规则都必须与本项目实际代码一致——**宁可少写，不写臆测**。写错的规则比没有规则更危险，AI 会严格执行它。
> 4. **跨文件同源内容以本文件为权威**：技术栈表（§3）、构建命令（§6）、Skill 触发表（§13）在 `AGENTS.md`/`MEMORY.md` 中的副本是速览，改动先改这里、再同步过去。
> 5. 本文件随项目演进持续更新（重大决策、纠偏、踩坑后及时回写），并更新顶部日期。

---

## 1. 回复与沟通

- 所有回复使用中文，专业、简洁、高效。
- 不使用表情符号，除非我明确要求。
- 需求不明确时先用 AskUserQuestion 澄清，不要猜测。
- 修改文件前先说明计划、等待确认；每次改动控制在最小范围；改完简要说明改了什么、为什么。

## 2. 项目概况

> 说明：用 3~8 行讲清"这是个什么系统、核心业务流程是什么、代码放在哪"。目标是让 AI 第一次进仓库就有正确的心智模型，防止它把项目当成另一类系统。

- 一句话定位：{{本系统是做什么的，服务谁}}
- 核心业务流程：{{主流程，如：提交 → 审核 → 处理 → 验收 → 关闭；有多条产品线/流程时全部列出，防止 AI 只认识其中一条}}
- 目录布局：后端 `{{后端目录，如 backend/}}`（{{后端栈一句话}}）；前端 `{{前端目录，如 frontend/}}`（{{前端栈一句话}}）
- 本地运行方式概述：{{如：Docker 跑数据库 + 后端，本机 Node 跑前端}}；详见 `README.md`
- 多 Agent 协作角色与硬规则见 `AGENTS.md`；长期记忆见 `MEMORY.md`

### 2.1 核心领域模型 / 状态机（可选）

> 说明：如果项目有状态机、审批流、计费规则等核心领域规则，在这里画出主干（文本图即可），并**必须**注明权威源文件。经验：文档里的图只作导览，AI 改流转逻辑前必须先读权威代码——把这条写死，防止它凭图臆断迁移条件。

```
{{状态机/流程文本图}}
```

- **合法迁移/规则以 `{{权威源文件路径}}` 为准**，本图仅导览；改动前先读源码，不要凭本图臆断。
- {{其它领域约定，如"用户可见状态 ≠ 内部状态"、"XX 不是终态"等易误解点}}

## 3. 技术栈基准（务必记准，勿套用其它项目经验）

> 说明：这张表是防"AI 拿别的项目/开源惯例往这套"的核心防线，也是全项目**唯一权威版本**（`AGENTS.md` 的技术基准表是它的速览副本）。第三列专门写"容易搞错的点"——版本陷阱（如 Spring Boot 2.x 是 `javax.*` 不是 `jakarta.*`）、"没有什么"（无统一响应包装、无某状态库）比"有什么"更重要，AI 最常犯的错是臆造项目里不存在的封装。表中示例多为 Vue/Java 味，React/Go/Python 等项目替换为对应惯例即可（如：函数组件 + Hooks、`hooks/` 目录、snake_case）。

| 层 | 技术 | 易错点 / 约束 |
|---|---|---|
| 后端框架 | {{框架 + 精确版本}} | {{版本相关陷阱}} |
| 语言/运行时 | {{JDK/Node/Python 等精确版本}} | {{语法特性可用性}} |
| 持久层 | {{数据库 + ORM/驱动}} | {{如：文档库非关系型 / 迁移工具是什么}} |
| 认证鉴权 | {{JWT/SSO/框架方案}} | {{鉴权写法约定}} |
| 响应/分页约定 | {{统一包装类或"无包装"}} | {{明确写"无 XX，不要臆造"}} |
| 前端框架 | {{框架 + 写法约定}} | {{如：只用 Composition API / 只用函数组件 + Hooks}} |
| UI 库 | {{库 + 版本}} | 禁止混用其它 UI 库 |
| 状态管理 | {{方案或"无"}} | {{"无"时全局态的替代写法}} |
| 路由 | {{方案 + 权限控制方式}} | {{如 meta 权限码 / 路由守卫}} |
| 包管理 | {{npm/pnpm/yarn 等，写死一个}} | 明确"不是"其它几个 |
| i18n | {{方案或"无"}} | {{强制程度}} |
| 测试 | {{前后端测试框架}} | 新增/改动须配套测试 |
| 构建 | {{Maven/Gradle/Vite 等}} | 详见第 6 节命令 |

**后端模块速查（`{{后端源码根路径}}`）：**
{{`模块A`（职责）· `模块B`（职责）· …——一行一个域，让 AI 找代码不用全仓库扫}}

**前端关键路径（`{{前端源码根路径}}`）：**
- 页面：{{views/pages 目录与主要页面}}
- 类型：{{类型文件约定——类型统一放哪里}}
- 请求：{{API 层入口与约定，如"统一走 api/ 层的 request()，禁止组件内散写 fetch"；有特殊通道（流式/上传）单独注明}}
- 静态资源：{{目录与引用约定（public/assets、图标方案）}}
- 其它：{{composables 或 hooks / stores / i18n 等}}
- {{本地存储 key 等前端全局约定}}

## 4. 代码风格

**前端**
- 组件 PascalCase，变量/函数 camelCase，常量 UPPER_CASE；用 ES Module，禁止 CommonJS。
- TypeScript 项目开启 strict；禁止隐式 any，禁止用 `as` 断言绕过类型（确需断言处注明原因）。
- API 调用统一走 API 层，不在组件里散写请求。
- 展示文案统一走 i18n（若项目启用，见第 9 节），禁止硬编码。
- 避免不必要的注释和冗余的防御性 try/catch（信任框架边界）；但类型收窄与外部输入/边界校验该写就写。
- {{项目特有：引号/分号/格式化工具、DOM 操作约定等}}

**后端**
- 遵循该语言官方命名规范（{{如 Java/TS camelCase、Python snake_case、Go 导出符号 PascalCase}}）。
- 分层：{{如 Controller → Service → Repository/DAO}}，不跨层调用。
- 事务边界落在 {{Service 层}}；跨多个写操作必须保证原子性；事务内不得包裹外部 HTTP/RPC 调用。
- 异常统一由全局处理器收敛为 {{错误结构/错误码方案}}，不在各层裸抛未包装异常。
- 日志走 {{日志框架}}，禁止绕过日志框架用 `System.out.println`/`print`/`console.log` 裸打；**密钥/token/Authorization 头/完整 SQL 绝不写入日志**（完整脱敏清单见第 7 节）。
- {{项目特有：鉴权注解写法、包名陷阱、DTO/VO 约定等}}

## 5. 工作流程（强制）

- 纪律优先于直觉，分阶段隔离上下文工作。
- 多文件变更、新功能、架构调整 → **先进入计划模式**，输出清晰步骤 + 风险点，经我确认后再执行。
- 修改前必须先 Read 文件，不要猜测代码；优先用 Edit 精准替换，不要整文件 Write。
- 标准链路：`需求 → 头脑风暴/方案澄清 → 写实施计划 → 批准 → 实施（新增/修 bug 按 TDD：先写失败测试再实现） → 完成前验证 → 完成`。
- {{若团队使用特定工作流插件/skill（如 Superpowers、公司 skill 套件），在此注明并见第 13 节}}

## 6. 构建与运行命令

> 只在需要验证时运行读/构建/测试命令；**禁止**执行安装、部署、git 写操作等高危命令（第 12 节）。本节为命令的权威版本，`AGENTS.md`/`MEMORY.md` 中的副本须与此同步。
> 说明：命令要写到可直接复制执行的精确程度（含目录、参数）；有"改了 A 必须重做 B 才生效"的联动（如改 Java 须重打包重建容器），必须显式写出来——这是 AI 最容易漏的一类。

- 后端构建：`{{命令}}`
- 后端测试：`{{命令}}`（单测/集成为独立命令时分别列出）
- 启动依赖服务（数据库等）：`{{命令}}`
- 前端 lint：`{{命令}}`
- 前端开发：`{{命令}}`
- 前端构建（含类型检查）：`{{命令}}`（注意：构建与类型检查可能是两个独立门禁，都要跑）
- 前端测试：`{{命令}}`
- 重要联动：{{如：改后端代码后必须重新打包并重建容器，只刷页面不生效}}
- 开发代理/端口：{{dev 代理规则、前后端端口}}
- 环境区分（可选）：{{dev/staging/prod 各环境的配置差异要点与对应配置文件}}
- 演示/测试账号：{{账号与来源文件；无则删除本行}}

**常用环境变量（默认值来源见 {{配置文件路径}}）：**
- {{变量名：用途（默认值）}}

## 7. 安全与凭据（红线）

> 说明：**每个项目必须显式声明自己的密钥姿态，不能沿用其它项目的默认**——有的内网项目允许提交内部凭据，有的零明文入库，两种姿态是相反的。首行强制二选一，不允许写成"妥善保管"之类的含糊表述。

**本仓库密钥姿态（二选一，必须显式声明）：{{① 零明文入库：不提交任何明文密钥 / ② 允许提交内部凭据，仅限：<明确范围>}}。**

- 外部凭据的存放方式：{{如：加密入库 + 主密钥走环境变量 / 仅环境变量 / 密钥管理服务}}；主密钥不得写入源码、日志、镜像。
- **前端构建期环境变量（`VITE_`/`NEXT_PUBLIC_` 等前缀）会被原样打进浏览器产物**：任何密钥/令牌不得放入前端 env。
- {{若存在本地开发绕过开关：写明开关名、默认值}}——绕过开关生产必须默认关闭且校验失败即拒绝（fail-closed），理想做法是构建期排除，防止误开成永久后门。
- 失败原因与日志必须脱敏，以下一律不回显给前端或写入日志：**密码、token、session id、Cookie、Authorization 头、完整 SQL、PII（手机号/身份证/邮箱等）、私钥/证书材料、含密码的连接串、内网主机名/内网 URL**。
- 出站请求约束：URL 禁止携带 userinfo（`user:pass@`）；生产禁止 http 明文降级；禁止指向环回地址、内网私有网段、云元数据端点（如 169.254.169.254）——SSRF 防护的重点是目标地址，不是协议。
- 发现安全问题（XSS、注入、未验证输入、密钥泄漏、SSRF 等）→ 立即指出并给出修复方案。
- 涉及用户输入 / 鉴权 / 外部接口 / 密钥 / 数据库操作时，主动做安全评审（见第 13 节）。

**自动化红线（基线固定，清单按项目补充）：**

- AI 生成的结论一律为建议，最终审批/状态推进必须人工确认；**绝不自动执行不可逆操作或对外部系统的写操作**。
- {{项目特有的禁止自动执行清单，如：绝不自动执行 SQL / 绝不自动向外部系统提交工单}}
- {{外部集成默认开关状态与关闭时的降级行为}}

## 8. 权限与接口契约

> 说明：有 RBAC/权限码体系的项目保留本节；没有的删除权限部分，但 API 契约权威源必须保留。

- **后端鉴权是唯一权威安全边界**：每个受保护接口必须在后端独立强制鉴权；前端路由/按钮权限码仅控制展示（UX），任何情况下不得作为唯一或最终的安全边界。
- **API 契约权威源：`{{OpenAPI 文件 / 共享类型包 / 接口定义目录}}`**，由 Architect 维护；接口变更先改契约源、再改实现，前后端以契约源为准同步。
- 角色：{{角色清单}}；菜单/入口的动态控制方式：{{如何按权限返回}}。
- **权限码全集以 `{{权威源文件}}` 为准，下表仅按域列高频项，不是全集**；新增接口/菜单时以源码对照。
- {{高频权限码分域清单}}
- 新增受保护接口时，后端鉴权码与前端路由/按钮权限码**必须一致**。
- 权限判定默认拒绝（default-deny）；如存在兜底放行规则（{{如：具备管理权限即放行子权限}}），必须显式列出并单独复核。
- {{权限体系的隐性耦合规则，如：菜单可见性影响接口鉴权——这类"改 A 会牵动 B"的隐性契约必须写出来}}
- 权限码/接口路径/响应字段变更属于契约变更，须通知 Architect（见 `AGENTS.md`）。

## 9. 国际化（可选）

- 前端必须支持 {{语言清单}}。新增的页面、菜单、按钮、表格列、表单标签、占位符、弹窗标题、校验信息、toast、空状态**一律**走 i18n，禁止硬编码展示文案。
- 语言切换器位置与持久化：{{如 localStorage key}}。
- 后端业务数据的翻译边界：{{哪些按后端原样显示、哪些前端翻译}}。

## 10. 前端布局约束（可选，适用于固定外壳的后台/应用型布局）

- 登录后浏览器页面不作为主滚动容器；侧栏/头部固定，内容在工作区内部滚动。
- 需要内部滚动的元素，其**每一级** flex 祖先都要设 `min-height: 0`（默认 `min-height:auto` 会阻止收缩）；grid 场景用 `minmax(0, 1fr)`。
- 长列表须分页或虚拟滚动；分页控件属于列表容器而非浏览器页面。
- 弹窗/抽屉定义相对视口的最大高度，长内容在其 body 内部滚动，页脚主操作始终可达。
- 布局改动需在桌面宽度与窄屏各验证一次{{；启用 i18n 时再验证语言切换后主流程可用}}。

## 11. Git 操作策略（严格限制）

- **禁止** Claude 自动执行 `git commit`、`git push`、`git rebase`、`git reset` 等写操作。
- 允许：`git status`、`git diff`、`git log`、`git show`。
- 禁止修改 git hooks、CI 配置（`.github/workflows` 等）与 `.claude` 权限/设置文件；禁止用 `--no-verify` 等方式绕过钩子与门禁。
- 变更就绪时用中文提醒，例如："文件已修改完成，可手动执行 `git add . && git commit -m '…'`"。
- **Commit 规范：{{中文/英文，二选一并与仓库现有历史保持一致}} Conventional Commits**：`type(scope): 描述`，类型取 `feat/fix/refactor/docs/test/style/perf/chore`，首行 < 50 字符。
- 分支与 PR 策略（可选）：{{分支命名、基线分支、PR 流程；无则写"直接提交到 <分支>"}}。

## 12. 永远禁止（红线）

- 执行依赖安装/变更与部署发布类命令：{{本项目对应命令，如 `npm/pnpm/yarn install`、`mvn install/deploy`、`pip install`、`go get`、`cargo add`}}。
- 执行 `git push`、`git reset --hard`、`rm -rf` 等破坏性命令。
- 向外部地址发送仓库内容、凭据或环境变量（`curl`/`wget`/`Invoke-WebRequest` 外发、外部 webhook、粘贴板/匿名上传服务）。
- 引入依赖清单（`package.json` / `pom.xml` / `go.mod` / `pyproject.toml` 等）之外的新依赖（须我明确同意）。
- 违反第 7 节的密钥姿态。
- 未通知 Architect 就修改接口路径、响应字段或权限码。
- {{项目特有红线}}

## 13. 必须使用的 Skills

> 说明：本表为权威版本（`AGENTS.md` 的触发表是其子集副本，须同步）。按团队实际安装的插件/skill 调整名称；没有对应 skill 的场景删除该行，不要留无法调用的名字。**若公司有官方开发 skill（脚手架/门禁类），公司 skill 优先于通用 skill，冲突时先问用户。**

| 场景 | Skill |
|---|---|
| 新功能 / 需求变更 / 技术选型 / 重构前 | {{brainstorming 类 skill}} |
| 进入实施前 | {{writing-plans 类 skill}} → 计划模式 |
| 遇到 bug / 测试失败 / 异常行为 | {{systematic-debugging 类 skill}} |
| 声称"完成"前 | {{verification 类 skill}} |
| 新增功能 / 修 bug / 重构 | {{TDD 类 skill}}（红-绿-重构） |
| 涉及输入 / 鉴权 / 接口 / 密钥 / 数据库 | {{security-review 类 skill}} |
| {{公司框架场景：建项目/写页面/联调/建后端}} | {{公司官方 skill，如有}} |

## 14. Memory 习惯

- 架构决策、反复出现的偏好、外部集成约定 → 写入 `MEMORY.md`。
- 临时任务细节不写入长期记忆；错误的记忆要删除而不是堆积。
- 重要结论后可建议："是否记录到 MEMORY.md？"

用 AskUserQuestion 随时澄清需求。
