# npm registry：默认使用公司内部 npm 私服（用于统一依赖来源、加速下载、增强安全性）
registry=http://192.168.10.107:8081/repository/npm-public/
# registry=http://localhost:4873/

# 允许在 workspace 根目录执行 pnpm 命令（跳过“误操作”安全校验）
# 适用于需要在仓库根统一安装工具链或运行脚本的场景
ignore-workspace-root-check=true

# 安装依赖时固定为精确版本号（例如 1.2.3），避免自动使用 ^ / ~ 造成的隐式升级
save-exact=true

# 设置依赖的最小发布时间冷静期（单位：分钟）
# 此处为 10080 分钟（7 天），用于规避新发布版本的不稳定性和潜在安全风险
minimum-release-age=10080

# 冷静期白名单：以下私有/内部包不受 minimum-release-age 限制
# 适用于企业内部维护、可控风险的依赖
minimum-release-age-exclude=@q-cli/*,@q-ui/*,@q-tools/*,q-*,quectel-*

# 禁用“羞耻式提升（shamefully hoist）”
# 保持 pnpm 的严格依赖隔离策略，避免幽灵依赖和 monorepo 中的隐式依赖问题
shamefully-hoist=false
